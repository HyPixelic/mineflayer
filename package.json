{
  "author": "Pixelic",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm tsc",
    "test": "pnpm build && node --test test/commands.js",
    "test:live": "node --env-file=.env test/index.js",
    "prettier:check": "pnpm dlx prettier . --check",
    "prettier:write": "pnpm dlx prettier . --write"
  },
  "dependencies": {
    "mowojang": "^0.8.2"
  },
  "peerDependencies": {
    "mineflayer": "^4.34.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
